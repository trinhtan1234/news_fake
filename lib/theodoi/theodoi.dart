import 'package:flutter/material.dart';

class TheoDoi extends StatelessWidget {
  const TheoDoi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Đang cập nhật'),
    );
  }
}
